services:
  - type: web
    name: learning-portal
    env: node
    buildCommand: cd src && npm install
    startCommand: cd src && npm start
    envVars:
      - key: NODE_ENV
        value: production